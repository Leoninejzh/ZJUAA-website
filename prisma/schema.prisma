datasource db {
  provider = "postgresql" // 必须改为 postgresql（与 Supabase DATABASE_URL 匹配）
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  // 确保包含 linux 兼容项以解决你之前看到的 update 提示
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

model SiteSettings {
  id        String   @id @default(cuid())
  key       String   @unique
  value     String
  updatedAt DateTime @updatedAt
}

model UploadedImage {
  id        String   @id @default(cuid())
  filename  String
  url       String
  alt       String?
  category  String?
  createdAt DateTime @default(now())
}

model Article {
  id            String    @id @default(cuid())
  title         String
  content       String
  coverImageUrl String?
  type          String
  eventDate     DateTime?
  sortOrder     Int       @default(0)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model Donation {
  id             String   @id @default(cuid())
  amount         Float
  name           String
  email          String
  graduationYear String
  major          String
  message        String?
  paymentMethod  String
  createdAt      DateTime @default(now())
}
